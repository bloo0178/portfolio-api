{
  "name": "portfolio-api",
  "version": "1.0.0",
  "description": "API to expose project data contained within an AWS DynamoDB table. Created using the Serverless framework. Deployed to AWS.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "start-dev": "serverless offline start --stage dev",
    "start-prod": "serverless offline start --stage prod"
  },
  "author": "Jeff Bloom",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.457.0",
    "body-parser": "^1.19.0",
    "dotenv": "^8.0.0",
    "express": "^4.17.0",
    "serverless-http": "^2.0.2"
  },
  "devDependencies": {
    "aws-sdk-mock": "^4.4.0",
    "jest": "^24.8.0",
    "serverless-offline": "^4.10.3",
    "supertest": "^4.0.2"
  }
}
